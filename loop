<!doctype html>
  <html>
    <body>
        <P> finding the largest number with help of argument</P>
        <p> id="demo"<>\<p>
             
            <script>
            \\ for (intialize); condition ;increament\decreament)
            function findmax(){
                var max=0;
                for (i=0);arguments(i)
                if (arguments){i}max )
                max= arguments{i}



document.getElementById("demo"). innerHTML=findmax(8 4 2 6 9 75 )
        
                
            
        
